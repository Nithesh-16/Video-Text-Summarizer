services:
  - type: web
    name: video-text-summarizer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run TranscriptIQ/Home.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
    plan: free 